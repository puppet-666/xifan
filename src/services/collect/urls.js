import baseUrl from '../baseUrl'

export default{
    getCollect:baseUrl + '/collect',
    isCollect:baseUrl + '/collect',
    delCollect:baseUrl + '/collect/delcollect',
    addcollect:baseUrl + '/collect/addcollect'
}