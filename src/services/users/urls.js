import baseUrl from '../baseUrl'

export default {
    userLogin:baseUrl +'/users/login',
    userReg:baseUrl + '/users/reg',
    changeName:baseUrl + '/users/changeName',
    changePassword:baseUrl + '/users/changePassword',
    searchName:baseUrl +'/users/'
}